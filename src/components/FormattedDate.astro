---
interface Props {
	date: Date;
	lng: string;
}

const { date, lng } = Astro.props;

---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(lng, {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
